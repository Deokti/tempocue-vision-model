# Сверка отрисовки: Data Dragon против настоящей миникарты

Ступень 1 программы. Статус: **фаза A завершена 24 августа 2026 —
подтверждено с уточнением источника**: значок миникарты воспроизводится из
официальных артов с остатком внутри шумового пола, но исходник — circle-иконка
из ассетов игры (Community Dragon), а не квадратный портрет Data Dragon.
Фаза B (родное разрешение) ждёт снимков из основного репозитория.

## Вопрос

Вся конструкция вложения стоит на допущении: портрет из Data Dragon — тот же
рисунок, который игра кладёт на миникарту, только уменьшенный и обработанный.
Пиксельное сравнение даёт максимум `0,94` — «похоже, но не тождественно».
Сверка обязана превратить «синтетика почему-то другая» в измеренные параметры
либо доказать тождество источника.

## Метод

Берём настоящий вырез значка из кадра корпуса (позиции известны из ручной
разметки) и пытаемся воспроизвести его синтезом из портрета Data Dragon
(120×120), перебирая параметры отрисовки. Итог по каждому значку — **остаточное
расхождение**: сколько разницы осталось после того, как объяснили всё, что умели.

Цепочка отрисовки трёхзвенная, оба уменьшения моделируются:

```
Data Dragon 120×120 → значок родного размера (16–65 px, ≈7,8 % стороны карты)
                    → канонические 320 (значок ≈25 px) = домен корпуса
```

Перебираемые параметры: родной размер значка, субпиксельный сдвиг, ресемплер
(ближайший/билинейный/бикубический/Ланцош/площадной), толщина и цвет командного
кольца, размытие, гамма (смешение в sRGB или в линейном свете), режим
альфа-канала на крае круга.

Сравнение — только по внутреннему кругу портрета, кольцо и фон исключаются
маской. Отчёт даёт остаток двумя зонами: весь круг и центральные ~11×11 —
личность фактически решают они, и расхождение там дороже.

## Шумовой пол — планка для критерия

Абсолютный остаток ничего не значит сам по себе. Сначала измеряется
изменчивость настоящего значка между кадрами: по записям
(`ReplaySequences`, ~24 кадра за ~3 с) — один и тот же чемпион против самого
себя, отдельно для неподвижных (Jinx, Annie) и движущегося (Darius; движение
рисуется размытым и даёт худший случай). Дополнительно — повторы чемпионов
между кадрами корпуса одного матча.

## Два режима подбора

- **индивидуальный** (каждому значку свои параметры) — отвечает «способен ли
  Data Dragon в принципе объяснить эти пиксели»;
- **общий** (один набор параметров на все значки) — отвечает «можно ли эти
  параметры зашить в генератор синтетики». Общий набор — фундамент ступени 2.

## Критерии решения (записаны до запуска)

- **Подтверждено**: индивидуальный остаток в пределах шумового пола для
  подавляющего большинства чистых значков (перекрытые и светящиеся не
  участвуют — им сравниваться не с чем), и общий набор параметров теряет к
  индивидуальному немного. Числовые пороги дописываются в этот документ после
  замера шумового пола и **до** запуска подбора параметров.
- **Опровергнуто/частично**: у остатка есть систематическая часть — карта
  разницы покажет, в каких чемпионах или зонах значка она сидит; решение о
  следующем шаге по этой картине.

Урок конвейера «мерить отрыв, а не балл» действует и здесь: выводы — только из
сравнения с шумовым полом, не из абсолютных чисел сходства.

## Ограничение области вывода

Все нынешние кадры и записи сохранены в канонических 320×320; родные пиксели
утрачены. Поэтому фаза A отвечает только про канонический домен. Фаза B — та же
сверка по вырезам родного разрешения — станет возможна, когда основной
репозиторий научится сохранять снимки в родном размере области миникарты
(требование записано в own-model-plan.md). До этого утверждения о родном
домене — догадки.

## План работ фазы A

1. ~~Читалка форматов `.tempocue-vision` / `.tempocue-sequence`~~ — сделано,
   проверено глазами (`tools/inspect_corpus.py`).
2. ~~Отбор значков и трекинг по записям~~ — сделано: ручные стартовые позиции
   в `annotations/sequence-seeds.json`, трекинг локальным поиском NCC
   (`src/tcvm/matching.py`), дорожки проверены глазами.
3. ~~Замер шумового пола; фиксация числовых порогов~~ — сделано, ниже.
4. ~~Загрузчик Data Dragon~~ — сделано: `src/tcvm/ddragon.py` (кэш в
   `data/ddragon/<версия>/`, версия фиксируется в каждом отчёте);
   `tools/inspect_ddragon.py` проверен глазами на Data Dragon 16.16.1 —
   все 31 чемпион разметки корпуса найдены, включая Zaahen и Ambessa,
   личности портретов совпадают с настоящими вырезами.
5. ~~Отрисовщик с параметрами и перебор~~ — сделано: `src/tcvm/render.py`,
   `tools/fit_render.py`; результаты ниже.
6. ~~Отчёт: чистое множество, вердикт по порогам, перевод сверки на
   circle-иконки~~ — сделано, вердикт ниже.

## Результаты

### Этапы 2–3: шумовой пол (23 августа 2026)

Инструмент — `tools/measure_noise_floor.py`; метрика — расхождение `1 − NCC`
(NCC — нормированная кросс-корреляция с вычитанием среднего) по внутреннему
диску радиуса 10 и по центральному квадрату 11×11; вырезы 25×25, положения
целочисленные — субпиксельный остаток входит в измеряемый шум.

Запись `seq-darius-movement` (24 кадра, соседние пары):

| Группа | n | Медиана | p95 | Максимум |
|---|---|---|---|---|
| малоподвижные, диск | 69 | 0,0000 | 0,139 | 0,182 |
| малоподвижные, центр 11×11 | 69 | 0,0000 | 0,103 | 0,130 |
| движущийся (Darius), диск | 23 | 0,146 | 0,301 | 0,338 |
| движущийся (Darius), центр 11×11 | 23 | 0,168 | 0,342 | 0,373 |

Корпус, пары одного чемпиона между кадрами одного матча, после выравнивания
поиском в окне ±3 px (без выравнивания измеряется разброс центров ручной
разметки, а не отрисовка, — медиана была бы 0,67): чистые пары легли в
0,000–0,194 (Ambessa 0,000; Janna 0,032; Malzahar 0,032; Teemo 0,036;
Sett 0,062; Shen 0,099; Blitzcrank 0,100; Poppy 0,134; Anivia 0,136;
Morgana 0,194; Sett 0,193). Все пары выше 0,3 объяснены известными дефектами
кадров: Tristana за Blitzcrank (виден только ободок), Diana у края карты,
перекрытые Sona и Ziggs, — в шумовой пол они не входят.

Главные находки:

1. **Идеально неподвижный значок повторяется байт в байт.** Zaahen: 23 пары
   подряд с расхождением ровно 0. Ambessa между двумя кадрами матча — тоже 0.
   Цифровой захват не имеет шума сенсора; весь шумовой пол создаётся движением
   и субпиксельными сдвигами, а не «шумом» как таковым.
2. **Субпиксельное «дыхание» малоподвижного значка** даёт до 0,18 на диске.
3. **Размытие движения** — главный источник: у Дариуса медиана 0,15 и хвост
   до 0,34. Это же объясняет устойчивость 79 % кадров у конвейера.

### Пороги решения (зафиксированы до подбора параметров)

Для фазы A, маска — внутренний диск радиуса 10, метрика `1 − NCC`:

- **Подтверждено**, если после индивидуального подбора параметров остаток
  синтеза против настоящего выреза **≤ 0,20** не менее чем для 90 % чистых
  неподвижных значков (0,20 = p95 малоподвижных с запасом до худшего чистого
  корпусного случая), и общий набор параметров проигрывает индивидуальному
  по медиане остатка не более чем на 0,05.
- Для вырезов с размытием движения допускается остаток **≤ 0,35**; в основной
  критерий они не входят и отчитываются отдельно.
- **Опровергнуто/частично**, если остаток выше порога у заметной доли чистых
  значков и карта разницы показывает систематическую зону или системных
  чемпионов-нарушителей.

### Этап 5: подбор параметров (23 августа 2026)

Инструмент — `tools/fit_render.py`: по каждому из 60 размеченных вырезов
перебор параметров отрисовки (грубая сетка → уточнение субпикселя), центр
выреза предварительно уточняется совмещением с наивным синтезом в окне ±3 px.
Тройки «настоящий | синтез | разница» — `out/render-fit/`. Data Dragon 16.16.1.

**Чистые значки объясняются полностью.** Остатки чистых вырезов легли в
`0,008`–`0,08` — многократно ниже порога 0,20 и внутри шумового пола
(примеры: Teemo `0,008`–`0,020` в четырёх кадрах, Anivia `0,009`,
Malzahar `0,009`, Diana `0,010`, Janna `0,015`, Ambessa `0,015`,
Sona `0,017`, Garen `0,023`, Tristana `0,025`, Blitzcrank `0,026`).
Победившие параметры согласованы между вырезами: портрет берётся целиком
(crop 1,00), родной размер значка 32–43 px (совпадает с ожиданием для карты
~452), ресемплер bilinear/bicubic, смешение в **sRGB, не в линейном свете**,
размытие 0–0,6.

**Главная находка: у части чемпионов игра рисует другой арт.** Sett провалился
во всех пяти кадрах двух разных матчей (`0,54`–`0,72`), Morgana — в четырёх
(`0,28`–`0,69`), при том что сами с собой между кадрами они совпадают
(повторы `0,06` и `0,19`). Разница видна глазами: у настоящего значка Sett
другая поза (рука у подбородка), у Morgana — другое кадрирование лица.
Эксперимент с игровыми circle-ассетами (Community Dragon,
`assets/characters/<champ>/hud/<champ>_circle_<skin>.png`) доказал причину:
**базовая circle-иконка** объясняет те же вырезы с остатком `0,028` (Sett) и
`0,079` (Morgana) — против `0,61`/`0,28` у квадратного портрета Data Dragon.
То есть миникарта использует отдельный circle-арт, который у большинства
чемпионов совпадает с квадратным портретом, а у части — нет.

Следствия для программы:

- **источник значков для прототипов и синтетики — игровые circle-иконки**
  (Community Dragon), а не квадратные портреты Data Dragon; для большинства
  чемпионов они совпадают, но полагаться на это нельзя;
- circle-иконки существуют **по одной на скин**; во всех наших кадрах побеждает
  базовая `_0`. Показывает ли миникарта скиновую иконку при игре на скине —
  по нашим данным неизвестно (открытый вопрос; Live Client Data API отдаёт
  skinID участников, так что механизм подстановки при необходимости есть);
- прочие высокие остатки объяснены визуально известными дефектами кадров:
  перекрытия (кадр 06 — Tryndamere/Darius/MonkeyKing, Sett за соседом,
  Tristana за Blitzcrank), свечение, край карты. Sion в кадре 07 не объяснён
  ни square (`0,44`), ни circle (`0,53`) — вырез тёмный с красным свечением,
  кандидат в грязные; разобрать на этапе 6;
- общий набор параметров по грубой сетке (все 60 вырезов, включая грязные):
  crop 1,00, native 27, bilinear, sRGB, blur 0 — медиана остатка 0,123;
  пересчитать по чистому множеству и с circle-источником на этапе 6.

### Этап 6: чистое множество и вердикт (24 августа 2026)

Инструмент переведён на два источника: `src/tcvm/cdragon.py` качает
circle-иконки из ассетов игры (кэш `data/cdragon/<патч>/`),
`tools/fit_render.py` подбирает параметры по обоим источникам независимо и
берёт лучший. Circle-иконка существует **на каждый скин**; используется
базовая (`_0`).

**Чистое множество** объявлено в `annotations/corpus-exclusions.json`:
22 выреза исключены, у каждого — видимый глазами дефект в настоящем вырезе
(перекрытие чужим значком или кольцом, линия рамки камеры, свечение, край
карты, эффект), проверенный по тройке. Критерий исключения — картинка, а не
величина остатка: исключать по числам значило бы подгонять критерий под
результат. Исключённые остаются в подборе как отрицательный контроль.

**Результат** (Data Dragon 16.16.1 / Community Dragon 16.16):

| | n | Медиана | p95 | Максимум |
|---|---|---|---|---|
| чистые вырезы | 38 | **0,033** | 0,089 | **0,113** |
| исключённые (контроль) | 22 | 0,446 | 0,842 | 0,955 |

- Порог «остаток ≤ 0,20 не менее чем у 90 % чистых» — выполнен: **38 из 38
  (100 %)**. Максимум чистых (0,113) ниже даже p95 шумового пола
  малоподвижных значков (0,139): синтез объясняет значок точнее, чем значок
  отличается сам от себя при субпиксельном дыхании.
- Общий набор параметров (по чистым, circle-источник): crop 1,00,
  native 37–43, bilinear/lanczos, **sRGB**, blur 0–0,6 — медиана остатка
  0,046, потеря к индивидуальному 0,013 при допуске 0,05 — выполнен.
  Конкретный ресемплер и родной размер внутри этого диапазона почти
  неразличимы по остатку; генератор всё равно обязан рандомизировать размер
  по всему измеренному диапазону 16–65 px.
- Чистые и грязные разделены разрывом (0,113 против 0,233+): метод различает
  «объяснимо» и «не объяснимо», подбор не «объясняет» перекрытия.
- Источники у чистых: 28 вырезов лучше объясняются circle, 10 — square;
  разница между источниками у совпадающих артов в пределах шума.

**Вердикт фазы A: подтверждено с уточнением источника.** Значок миникарты —
это circle-иконка из ассетов игры, отрисованная измеренной цепочкой
(портрет целиком, родной размер, bilinear-класс ресемплер, смешение в sRGB,
лёгкое размытие). Для большинства чемпионов circle совпадает с квадратом
Data Dragon, но у части (Sett, Morgana, Caitlyn — на нашем корпусе) арты
разные, поэтому **прототипы вложения и генератор синтетики строятся из
circle-иконок Community Dragon**.

Открытые вопросы, переходящие дальше:

- **скины — вопрос закрыт 24 августа 2026**: собраны 10 кадров Practice Tool
  на двух скинах (Lux «Боевая академия», Miss Fortune «Ватерлоо»,
  `replay-inbox`). Во всех чистых кадрах (6 из 10) побеждает **базовая**
  `circle_0` с остатком `0,010`–`0,045`, скиновая иконка проигрывает с
  разрывом ≥ `0,2`; выбросы объяснены глазами (значок у базы среди элементов
  интерфейса, кандидат не являлся значком). **Миникарта рисует базовую иконку
  независимо от скина** — галерея прототипов состоит только из базовых
  circle-иконок, skinID из Live Client Data не нужен;
- **фаза B**: те же измерения по вырезам родного разрешения, когда основной
  репозиторий научится сохранять родные снимки;
- Community Dragon — неофициальное зеркало ассетов; для поставки продукта
  условия использования предстоит проверить (лицензионный вопрос той же
  природы, что уже отложенные в плане).
